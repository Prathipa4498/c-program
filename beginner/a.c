
#include <stdio.h>

int main(void) {
	char a[20];
	int b[20];
	scanf("%s",&a);
	for(i=0;i<strlen(a);i++)
	{
		if(a[i]==I)
		b[i]=1;
		if(a[i]==V)
		b[i]=5;
		if(a[i]==X)
		b[i]=10;
	}
	int n=b[0];
	for(i=0;i<strlen(a);i++)
	{
		if(b[i]>=b[i+1])
		n=n+b[i+1];
		else
		n=
	
		
	}
	
}
